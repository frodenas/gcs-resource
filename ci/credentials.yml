---
gcs_resource_bucket_name: <The GCS bucket name to use when running the integration tests>
gcs_resource_versioned_bucket_name: <The versioned GCS bucket name to use when running the integration tests>
gcs_resource_json_key: |
  <The contents of your GCS Account JSON Key file>
docker_username: <The Docker username to authenticate with  with the Docker Hub when pushing the image>
docker_password: <The password to use when authenticating with the Docker Hub>
